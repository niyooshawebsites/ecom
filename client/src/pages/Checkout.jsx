import Layout from "../comps/Layout";
import CheckoutForm from "../comps/CheckoutForm";

const Checkout = () => {
  return (
    <Layout>
      <CheckoutForm />
    </Layout>
  );
};

export default Checkout;
